# Bidirectional Linking Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Link two items and see the connection from both detail pages — manual, undirected, user-scoped links.

**Architecture:** New `links` table storing one canonicalised row per pair (`a_item < b_item`, CHECK-enforced, cascade on item delete). Contract-first: three endpoints in `openapi.yaml` (`GET/POST /items/{id}/links`, `DELETE /items/{id}/links/{toId}`), regenerated Go + TS. Handlers canonicalise via `bytes.Compare` of the uuids and 404 on cross-tenant/missing (user-scoped `GetItem` checks). Web detail rail gains a Linked section (rows + unlink ×) and an inline picker fed by `GET /api/items` filtered client-side.

**Tech Stack:** Existing stack only — sqlc/pgx migration + queries, oapi-codegen, chi handlers, Next.js detail page + cookie-proxy routes. No new dependencies.

Spec: `docs/superpowers/specs/20260706-links-design.md`.

## Global Constraints

- Contract workflow: edit `openapi.yaml` → `task generate` (needs the good Go toolchain, see below) → implement handlers. Never hand-edit `internal/api/gen.go`, sqlc output, or `packages/api-client`.
- Every query user-scoped; cross-tenant/missing → 404 (indistinguishable).
- Errors `fmt.Errorf("doing x: %w", err)`; no banner comments; table-driven Go tests against real Postgres.
- **Toolchain:** prefix all Go commands with `env -u GOROOT /opt/homebrew/bin/go` from `apps/api` (goenv shim is broken). For `task generate`, instead run its three commands directly (Taskfile invokes bare `go`): `cd apps/api && env -u GOROOT /opt/homebrew/bin/go run github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen@v2.4.1 -config oapi.cfg.yaml ../../openapi.yaml`, then `env -u GOROOT /opt/homebrew/bin/go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.29.0 generate`, then from repo root `pnpm --filter @openmind/api-client generate`.
- **Test DB:** `docker compose up -d db` at repo root; DB tests default to `postgres://openmind:openmind@localhost:5433/openmind_test` (db `openmind_test` may need `docker compose exec -T db psql -U openmind -c "CREATE DATABASE openmind_test"`). Run suites with `-p 1`.
- Web: design tokens only; data via the cookie-proxy `/api/*` routes; strict TS.

---

### Task 1: Migration + contract + store + handlers + Go tests

**Files:**
- Create: `apps/api/internal/store/migrations/0010_links.sql`, `apps/api/internal/store/queries/links.sql`, `apps/api/internal/api/links.go`, `apps/api/internal/api/links_test.go`
- Modify: `openapi.yaml` (three endpoints under a new `/items/{id}/links` + `/items/{id}/links/{toId}` path)
- Generated (never by hand): `apps/api/internal/api/gen.go`, `apps/api/internal/store/db/*`, `packages/api-client/src/schema.d.ts`

**Interfaces:**
- Produces (Task 2 consumes via the TS client/proxies): `GET /items/{id}/links → 200 Item[]`; `POST /items/{id}/links {toId: uuid} → 201 Item[]`; `DELETE /items/{id}/links/{toId} → 204`. operationIds: `listItemLinks`, `createItemLink`, `deleteItemLink`.

- [ ] **Step 1: migration `0010_links.sql`** (verbatim from the spec):

```sql
CREATE TABLE links (
    user_id    uuid NOT NULL REFERENCES users(id),
    a_item     uuid NOT NULL REFERENCES items(id) ON DELETE CASCADE,
    b_item     uuid NOT NULL REFERENCES items(id) ON DELETE CASCADE,
    created_at timestamptz NOT NULL DEFAULT now(),
    PRIMARY KEY (user_id, a_item, b_item),
    CHECK (a_item < b_item)
);
CREATE INDEX links_b_idx ON links (user_id, b_item);
```

- [ ] **Step 2: contract.** In `openapi.yaml`, after the `/items/{id}` path block, add (match the file's existing style for parameters/refs — read neighbouring paths first):

```yaml
  /items/{id}/links:
    get:
      operationId: listItemLinks
      summary: Items linked to this item (both directions)
      parameters:
        - $ref: "#/components/parameters/ItemId"
      responses:
        "200":
          description: Linked items, newest link first
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Item" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createItemLink
      summary: Link this item to another (undirected, idempotent)
      parameters:
        - $ref: "#/components/parameters/ItemId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [toId]
              properties:
                toId: { type: string, format: uuid }
      responses:
        "201":
          description: Updated linked list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Item" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "404": { $ref: "#/components/responses/NotFound" }
  /items/{id}/links/{toId}:
    delete:
      operationId: deleteItemLink
      summary: Remove a link
      parameters:
        - $ref: "#/components/parameters/ItemId"
        - name: toId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204": { description: Removed }
        "404": { $ref: "#/components/responses/NotFound" }
```
IMPORTANT: check whether `components/parameters/ItemId` and `components/responses/NotFound`/`BadRequest` exist in this file — if the file instead inlines parameters/responses on each path (look at `/items/{id}` GET), mirror the existing inline style exactly instead of inventing refs.

- [ ] **Step 3: regenerate** (the three commands from Global Constraints). Expect `gen.go` to gain `ListItemLinks/CreateItemLink/DeleteItemLink` in `ServerInterface` — compile will fail until handlers exist. That's the failing state.

- [ ] **Step 4: store queries `links.sql`:**

```sql
-- name: CreateLink :execrows
INSERT INTO links (user_id, a_item, b_item)
VALUES ($1, $2, $3)
ON CONFLICT DO NOTHING;

-- name: DeleteLink :execrows
DELETE FROM links WHERE user_id = $1 AND a_item = $2 AND b_item = $3;

-- name: ListLinkedItems :many
SELECT i.* FROM links l
JOIN items i ON i.id = CASE WHEN l.a_item = $2 THEN l.b_item ELSE l.a_item END
WHERE l.user_id = $1 AND ($2 IN (l.a_item, l.b_item))
ORDER BY l.created_at DESC;
```
Re-run the sqlc generate command.

- [ ] **Step 5: handlers `links.go`.** Shape (adapt to the generated signatures):

```go
package api

import (
	"bytes"
	"errors"
	"log/slog"
	"net/http"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5"
	openapi_types "github.com/oapi-codegen/runtime/types"

	"github.com/rohithgilla12/openmind/api/internal/store/db"
)

// canonicalPair orders two item ids so the stored pair always satisfies the
// table's a_item < b_item invariant regardless of link direction.
func canonicalPair(x, y uuid.UUID) (uuid.UUID, uuid.UUID) {
	if bytes.Compare(x[:], y[:]) < 0 {
		return x, y
	}
	return y, x
}

// ownsItem resolves an item under the caller's user id; false covers both
// missing and cross-tenant (they must be indistinguishable).
func (s *Server) ownsItem(ctx context.Context, uid, id uuid.UUID) (bool, error) {
	_, err := s.store.Queries.GetItem(ctx, db.GetItemParams{UserID: uid, ID: id})
	if errors.Is(err, pgx.ErrNoRows) {
		return false, nil
	}
	return err == nil, err
}
```
`ListItemLinks`: ownsItem → 404 / `ListLinkedItems` → `[]Item` via `toAPIItem` (always an array, never null). `CreateItemLink`: decode body (max bytes reader like other handlers), parse `toId`, self-link → 400 "cannot link an item to itself", ownsItem BOTH ids → 404, canonicalPair → `CreateLink` (ignore rows — idempotent), respond 201 with the fresh `ListLinkedItems`. `DeleteItemLink`: ownsItem `{id}` → 404, canonicalPair → `DeleteLink`; rows==0 → 404, else 204.

- [ ] **Step 6: tests `links_test.go`** — follow the package's existing DB-test harness (see `desk_test.go`/`items_patch_test.go` for setup helpers). Table-driven where natural. MUST cover: link A→B then `GET B/links` contains A (bidirectionality); re-link A→B and B→A both idempotent (still one row, still 201); self-link 400; toId belonging to another user → 404 (create a second user); GET on missing id → 404; delete → 204 then re-delete → 404; deleting item A cascades (B's list empties, no FK error).

- [ ] **Step 7: run.** `docker compose up -d db` (+ create `openmind_test` if missing); `cd apps/api && env -u GOROOT /opt/homebrew/bin/go test -p 1 ./internal/api/... -run TestLink -v` green; `go build ./...` + `go vet ./...` clean; full `go test -p 1 ./...` green.

- [ ] **Step 8: commit** `feat(api): bidirectional item links — schema, contract, handlers`.

---

### Task 2: Web — Linked section + picker + proxy routes

**Files:**
- Create: `apps/web/app/api/items/[id]/links/route.ts` (GET, POST), `apps/web/app/api/items/[id]/links/[toId]/route.ts` (DELETE), `apps/web/components/LinkedSection.tsx` (client component)
- Modify: the item detail page (find it: `apps/web/app/item/[id]/page.tsx`) — render `LinkedSection` in the right-hand rail under the tags block

**Interfaces:**
- Consumes Task 1's endpoints through the proxies. Regenerated `@openmind/api-client` types are available; import `Item` type from the client if the detail page already does (mirror its imports).

- [ ] **Step 1: proxy routes.** Both files follow the existing `apps/web/app/api/items/[id]/route.ts` pattern exactly (read it first — Next 15 async params: `{ params }: { params: Promise<{ id: string }> }`). GET/POST forward to `/items/{id}/links`; DELETE to `/items/{id}/links/{toId}`; pass through status + JSON (204 has no body — return `new NextResponse(null, { status: res.status })`).
- [ ] **Step 2: `LinkedSection.tsx`** (client): props `{ itemId: string }`. On mount fetch `/api/items/${itemId}/links` → rows (title-or-host, mono card-type caption, click → `router.push('/item/'+id)`), each with an × (DELETE, optimistic removal, restore on failure). "+ Link" toggles a picker: input + list from `GET /api/items?limit=50` filtered client-side (case-insensitive substring on title/url), excluding `itemId` and already-linked ids; click a row → POST `{toId}` → replace list with the 201 response, close picker. Empty state: "No links yet — weave a thread." Use tokens from `@openmind/ui` exactly as the neighbouring detail-page components do (read them; no hardcoded colours), mono captions, hairline separators.
- [ ] **Step 3: mount it** in the detail rail under tags (read the page structure; pass the item id).
- [ ] **Step 4: gates.** `pnpm --filter web build` + `pnpm --filter web lint` green.
- [ ] **Step 5: commit** `feat(web): linked-items rail section with inline picker`.

---

### Task 3: Compose e2e + TODO

- [ ] **Step 1:** `OPENMIND_TOKEN=devtoken AI_PROVIDER=noop docker compose up -d --build api web`; wait for :3000/login → 200.
- [ ] **Step 2 (API e2e, Bearer):** create two items (`POST /api/items` with distinct urls) → ids A, B. `POST /api/items/A/links {toId:B}` → 201 array containing B. `GET /api/items/B/links` → contains A (bidirectional). Repeat POST → still 201, list length unchanged (idempotent). `POST /api/items/A/links {toId:A}` → 400. `DELETE /api/items/A/links/B` → 204; `GET /api/items/A/links` → `[]`; re-DELETE → 404. Record outputs.
- [ ] **Step 3 (web e2e):** with the cookie session (POST /api/auth to get om_token), fetch `/item/A` HTML → 200 (the Linked section is client-rendered; asserting the page renders + the proxy endpoints respond is sufficient — full browser drive optional if headless Chromium is handy).
- [ ] **Step 4:** `docker compose down`. Update `TODO.md`: links → Done (dated, evidence); note deploy pending VPS recovery if still down.
- [ ] **Step 5: commit** `feat(links): e2e evidence + TODO`.

---

## Self-Review notes
- Spec coverage: schema/canonical pair/CASCADE (T1S1), contract+regen (T1S2-3), store (T1S4), handlers incl. 400/404/idempotency (T1S5), bidirectional tests + cascade (T1S6), web rail+picker+proxies (T2), e2e (T3). ✓
- Consistency: operationIds `listItemLinks/createItemLink/deleteItemLink` used in both T1 and (via generated client) T2; canonicalPair bytes.Compare matches the SQL CHECK `a_item < b_item` (pg uuid comparison is bytewise — same order). ✓
- No placeholders. ✓
