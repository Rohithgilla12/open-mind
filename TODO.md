# TODO

> Solo tracker. Now = this week's evenings. Claude Code: read this at session start, update at session end. Graduates to GitHub Issues at OSS launch.

## Milestone 2 — "It feels magic"

> Opened 2026-07-04. M1 "Now" was empty; remaining M1 "Next" items are either user decisions (name/domain), deferred (multi-user auth — parked until laptop access), or triage candidates now folded into this backlog. M2 scope per PRD §10: colour search, NL query parsing, Lenses, reader mode, imports.

### Now
- (empty)

### Next
- [ ] Imports — remaining formats: **PDF capture** (upload → text extraction → item; needs a PDF text extractor dependency + asset storage of the PDF), **Omnivore JSON** export (zip-of-JSON schema). File import (HTML bookmarks / CSV / URL list) and RSS/Atom feed subscriptions shipped — see Done.
- [ ] Feeds follow-up: conditional GET (etag/last-modified) so a poll of an unchanged feed skips re-parsing the body — nice-to-have, not required for correctness (dedup already makes re-polls idempotent).
- [ ] MCP fast-follows (M3 shipped the read+capture core): **stdio transport** (`openmind mcp`) for co-located local self-host; **write tools** beyond save — `set_user_tags`, pin-to-Desk, create/delete Lens, delete item (wider blast radius — wants agent-safety thought); **Desk/Drift tools**; MCP **resources/prompts**; per-endpoint rate-limit tuning for `/mcp` (currently shares the 1/s+burst-10 per-IP bucket, and proxied traffic buckets under the web container IP). Minors from the branch review: tool-error strings use string-concat not `%w`, `internal/api/mcp.go` Backend methods return some store errors unwrapped, `list_lenses` silently drops a malformed stored rule (all cosmetic).

### Later
- [ ] Lossless AVIF metadata stripping / re-allow AVIF uploads (M1 carry-over — AVIF currently 415s at upload pending a metadata-strip implementation)
- [ ] Send to Kindle follow-ups: per-user Kindle address (currently one shared `KINDLE_EMAIL` for the whole instance — needs auth first); EPUB covers; carry in-article images into the EPUB (currently text-only); scheduled/recurring Lens digests (currently on-demand only)

## Milestone 1 — "Save & find" (deferred tail)

- [x] Decide: name — **Openmind confirmed by user 2026-07-07** (do a trademark/collision sweep before OSS launch: existing "OpenMind" projects exist in the AI space)
- [ ] Real auth (multi-user) — **IN PROGRESS 2026-07-07**: spec at `docs/superpowers/specs/20260707-auth-design.md` (Clerk web-only + per-user API keys + QR/short-code device connect; self-host token mode unchanged). Three plans: auth-api → auth-web → auth-clients.

## Done

### Milestone 4 — Send to Kindle
- [x] Send to Kindle (2026-07-07, `docs/superpowers/plans/20260707-kindle.md`) — send a single item, or a Lens's current matches as a multi-chapter digest, to a Kindle e-mail address as an EPUB. New stdlib-only `internal/epub` (deterministic zip ordering, no external deps) and `internal/mailer` (net/smtp wrapper, `Mailer.Send(ctx, Message)`) packages, both with their own unit tests including a fake-SMTP-server test (the contract-level authority for actual delivery). Contract: `POST /items/{id}/kindle`, `POST /lenses/{id}/kindle` → `202 {queued:true}`; `404` unknown/cross-tenant; `409` when `SMTP_HOST`/`SMTP_FROM`/`KINDLE_EMAIL` aren't all set (exact message: "kindle is not configured — set SMTP_HOST, SMTP_FROM and KINDLE_EMAIL"); `422` when there's no body to send. Checks run ownership → configured → body-present, most-specific-wins. New `send_kindle` River job (`internal/jobs/kindle.go`) builds the EPUB and calls the mailer; body-less item/lens is logged and treated as done (not retried), but any other failure (incl. SMTP) is retried up to 5 attempts — a retry can duplicate delivery, called out as an accepted caveat. Lens digest caps at the 25 most recent matching items with a body, one EPUB chapter each. **Web:** `KindleButton` (`apps/web/components/KindleButton.tsx`) — idle/sending/sent (auto-resets ~4s)/409/422/error states, mounted beside `PinButton`/`DeleteButton` on the item detail page and beside `DeleteLensButton` in the Lens header; body-less POST proxies at `/api/items/{id}/kindle` and `/api/lenses/{id}/kindle`. `docker-compose.yml` gained the six `SMTP_HOST`/`SMTP_PORT`/`SMTP_FROM`/`SMTP_USERNAME`/`SMTP_PASSWORD`/`KINDLE_EMAIL` passthroughs on the `api` service (previously missing, so the feature couldn't be configured through compose at all); `.env.example` and `docs/self-hosting.md` gained a **Send to Kindle** section (env table + Amazon "approve the sender, find your @kindle.com address" steps + caveats). **Tests:** Go unit tests for epub/mailer (incl. fake-SMTP send), DB-backed handler tests for all four status codes on both endpoints (`internal/api/kindle_test.go`). **Verified e2e 2026-07-06/07** against the local compose stack (`docker compose up -d --build api web`): unconfigured `POST /items/{id}/kindle` → `409` with the exact documented message; bogus id → `404` on both item and lens endpoints; a failed-extraction item (bad-domain URL, body stays empty) → `422 "item has no body to send"`; an empty-match Lens → `422 "lens has no matching items with a body to send"`. Restarted `api` with `SMTP_HOST=smtp.invalid SMTP_FROM=... KINDLE_EMAIL=...` → happy-path `POST` on both item and Lens digest → `202 {"queued":true}`, confirmed through the web proxy too (`:3000/api/...`, Bearer header); `river_job` rows for `send_kindle` show `state=retryable`, 2–3 attempts, each error `dialing smtp server: dial tcp: lookup smtp.invalid ... no such host` — the expected positive signal that the job actually attempts SMTP delivery and retries on failure. **Honest scope note:** real message delivery (successful SMTP handshake + Amazon acceptance) is *not* re-verified here — that's covered by Task 1's fake-SMTP-server Go test; this e2e only proves the queue→job→SMTP-attempt→retry path wires up correctly end to end, including through the web UI. **Milestone 4 is now complete** (bidirectional linking ✅, Dock v1 ✅, Send to Kindle ✅).

### Milestone 4 — bidirectional linking
- [x] Bidirectional linking (2026-07-06, `docs/superpowers/plans/20260706-links.md`) — link two items and see it from both sides. `links` table (migration `0010_links.sql`): one canonicalised row per pair (`a_item < b_item` CHECK; handler orders via `bytes.Compare`, provably same order as pg uuid `<`), `ON DELETE CASCADE`, both directions indexed. Contract-first: `GET/POST /items/{id}/links`, `DELETE /items/{id}/links/{toId}` in `openapi.yaml`, Go + TS regenerated. Create is idempotent (`ON CONFLICT DO NOTHING`, still 201 with the fresh list); self-link 400; cross-tenant/missing 404 everywhere. **Web:** detail-rail "Linked" section (rows + optimistic unlink ×, shared `.meta` captions) with an inline picker over recent items (excludes self + already-linked), via new cookie-proxy routes. 10/10 DB-backed Go tests (bidirectionality, both-direction idempotency, self-link, cross-tenant, delete/re-delete, cascade); compose e2e 2026-07-06: link A→B → 201 + B lists A, re-link idempotent, 400/204/404 paths, detail page renders the section. **Deployed + live-verified on production 2026-07-07** (link 201 → bidirectional list → 204 unlink; the VPS outage that evening self-resolved — load spike, no OOM). Follow-ups in Later: AI-suggested links, picker search over the full library, MCP link tools.

### Milestone 4 — Dock (macOS, v1)
- [x] Tauri dock v1 (2026-07-06, `docs/superpowers/plans/20260706-dock.md`) — macOS menu-bar companion in `apps/dock` (pnpm workspace member; Tauri v2 + Vite/React/TS). **⌘⇧S Quick Save**: `grab_frontmost_tab` (Rust, per-browser AppleScript — Safari + Chrome/Brave/Edge/Arc; Firefox unsupported by AppleScript) → POST `/api/items` with Bearer (reqwest, rustls, 15s timeout, off-runtime via `spawn_blocking`) → native notification; typed error mapping (automation-denied/unsupported-app/no-tab). **⌘⇧O Quick Find**: hidden frameless always-on-top panel (label `panel`, `macOSPrivateApi` for transparency, Accessory activation policy — no Dock icon) — debounced search via the new web `GET /api/search` proxy (`parse=true`), ↑/↓ + Enter opens `{instance}/item/{id}`, URL input → save mode, ⌘Enter saves URL/note, Esc/blur hides. Tray: Open panel / Save current tab / Settings (emits `open-settings`) / Quit. Settings stored in the **macOS Keychain** (`keyring` crate; token never logged), Validate & save mirrors the mobile persist-on-indeterminate semantics. Shortcut conflicts degrade (notify, tray still works) instead of aborting startup — review catch, as was the sync-osascript-on-async-runtime fix. 23 vitest + 2 cargo tests green; tsc/vite/cargo builds green. **Verification scope:** unit + build + app-launch verified headlessly (screen was locked); the interactive e2e (hotkeys, Automation consent, panel screenshot) is queued as a wake-up checklist item. Follow-ups in Next: pinned Desk bar, Win/Linux, launch-at-login, rebinding, DMG/notarisation.

### Milestone 2 — search fixes
- [x] Tag search stemming mismatch (2026-07-06, migration `0009_tag_stemming.sql`) — `search_tsv` indexed tags via `array_to_tsvector`, a literal/unstemmed lexeme, while `SearchFTS` queries with `websearch_to_tsquery('english', ...)`, which stems; multi-morpheme tags like "favourite" (→ query lexeme "favourit") never matched. Rebuilt the generated column to run `tags`/`user_tags` through `to_tsvector('english', ...)` like the other weighted fields. `array_to_string` is STABLE not IMMUTABLE, so the generated-column expression needed a small `IMMUTABLE` SQL wrapper function (`items_tags_to_text`) rather than a direct `to_tsvector('english', array_to_string(tags, ' '))`. Also fixed `ftsRowToItem`/`vecRowToItem` in `internal/search/search.go` to carry `PinnedAt`/`LastDriftedAt` into search results (previously dropped, so a pinned item's search-result card showed `pinnedAt: null`). sqlc regenerated (`internal/store/db/*.go`) since dropping+re-adding `search_tsv` moved its physical column position, changing `SELECT *` ordinal order. Verified: new DB-backed `TestTagSearchStemsEnglishTags` (`internal/search/search_test.go`) — matches on a stemmed user tag ("favourite"), a stemmed AI tag ("running"), a single-morpheme regression tag ("mine"), and asserts `PinnedAt` survives into the result; full `internal/search`/`internal/store` suites green.

### Milestone 3 — MCP server
- [x] MCP server (2026-07-06, `docs/superpowers/plans/20260706-mcp-server.md`) — a Model Context Protocol server at `<instance>/mcp` (Streamable HTTP) exposing the library to AI agents (Claude Desktop/Code). Six tools: `save_item`, `search_items`, `list_recent`, `get_item`, `list_lenses`, `run_lens`. Built with the official `github.com/modelcontextprotocol/go-sdk` v1.6.1 in a new isolated `internal/mcp` package (narrow `Backend` interface + compact tool DTOs + typed tools, unit-tested against a fake backend); `*api.Server` implements `Backend` via a `mcpBackend` adapter (avoids a name-collision with the generated REST `GetItem`/`ListLenses`), reusing the shared `capture` helper (extracted from `CreateItem`, capture-is-sacred preserved), `search.Run`, and `runLensRule`. Mounted on the chi router so it inherits Bearer auth; `/mcp` added to the rate-limiter allowlist (`guarded()`) — a whole-branch-review catch (the limiter is an allowlist, not blanket). No REST contract change (`/mcp` is JSON-RPC, a documented exception; `openapi.yaml`/codegen untouched). **Web:** since the API is never publicly exposed (web container is the single ingress), added a catch-all `apps/web/app/mcp/[[...path]]` proxy that streams the Streamable-HTTP/SSE response through to the API (forwarding `Mcp-Session-Id` both ways; Bearer or `om_token` cookie for auth) and exempted `/mcp` in `middleware.ts` so the cookie-gate doesn't 307 bearer requests to `/login`. Verified e2e 2026-07-06: local compose (noop) `initialize`→serverInfo openmind + all 6 tools + `save_item`→real pending row + `search_items` (understood "text benchmark") both directly (`:8080/mcp`) and through the web proxy (`:3000/mcp`, session continuity + SSE + 401 on no-token); **deployed to the VPS and confirmed live** through the tunnel at `https://openmind.gilla.fun/mcp` — session established, `tools/list`→6 tools, `list_recent`→real enriched production items (Cloudflare → cloudflared → web proxy → API → Postgres). `docs/self-hosting.md` gained an MCP section (`claude mcp add` + Claude-Desktop `mcp-remote` config); `apps/api/scripts/mcp-e2e.sh` drives the JSON-RPC flow. **Milestone 3 "It's alive" is now complete** (Drift ✅, Desk ✅, mobile share sheet ✅, MCP ✅).

### Milestone 2 — Drift (resurfacing)
- [x] Drift (2026-07-06, `docs/superpowers/plans/20260706-drift.md`) — a calm, full-screen daily resurfacing mode: reuses Desk's pin plumbing, adds a new `items.last_drifted_at timestamptz` column. `GET /drift` is read-only and returns `{items, total}` — up to 5 candidates (enriched, unpinned, not drifted in the last 30 days), ordered never-drifted-first then oldest-saved, plus the full candidate count. `POST /drift/{id} {"keep": bool}` always stamps `last_drifted_at=now()` (so the item won't resurface for 30 days) and, when `keep:true`, also pins it to the Desk; user-scoped, 404 on cross-tenant/missing. **Web:** `/drift` — the one deliberately dark, immersive full-screen screen (radial-gradient canvas, floating card, progress dots, mono "Drift · n of total today", Newsreader title, palette dots, "Saved n ago · never revisited" line), "Let go" (outline) / "Keep on my desk" (gold) buttons advance the batch and update a kept/let-go tally, with a completion state and an empty "caught up" state; sidebar Drift nav link now live (dropped its "soon" muted styling) — **all three primary nav screens (Desk / The Mind / Drift) are now live**, completing the core designed screens from the mockup. Verified e2e 2026-07-06 against the local compose stack (`docker compose up -d --build api web`, noop provider), reusing the existing library of 42 enriched items: `GET /drift` (bearer) → 5 candidates + `total:42`; `POST /drift/{A} {"keep":true}` → `200`, `GET /desk` then showed A, re-`GET /drift` excluded A and `total:41`; `POST /drift/{B} {"keep":false}` → `200`, `GET /desk` did **not** show B, re-`GET /drift` excluded B and `total:40`; `GET /drift` (web page, cookie session) → `200` HTML containing `Drift · 1 of 40 today`, confirming the live experience renders (not the empty state); home page HTML contains `href="/drift"`. Screenshot captured of the full dark Drift card UI. `docs/self-hosting.md` gained a Drift section.

### Milestone 2 — Desk (pinboard)
- [x] Desk (2026-07-06, `docs/superpowers/plans/20260706-desk.md`) — pin items to a small board separate from the full library. New `items.pinned_at timestamptz` column (partial index on pinned rows); `PATCH /items/{id}` gains optional `pinned bool` (combinable with `userTags` in the same call; neither present → 400, unchanged behaviour); `GET /desk` returns pinned items newest-pinned-first, user-scoped like every other query. `Item`/`ItemDetail` gain nullable `pinnedAt`. **Web:** `/desk` page (Grid of pinned items + empty state), `PinButton` on the detail page (PATCH via the existing `/api/items/[id]` proxy), sidebar "Desk" nav link now live (dropped its "soon" muted treatment; Drift stays muted). Verified e2e 2026-07-06 against the local compose stack (`docker compose up -d --build api web`, noop provider): `PATCH /api/items/{id} {"pinned":true}` on an already-enriched item → `200` with `pinnedAt` set; `GET /desk` (bearer, API) returned exactly that item; `GET /desk` (web page, cookie session) → `200` HTML containing the item's title, confirming the page renders pinned items; `PATCH {"pinned":false}` → `pinnedAt:null`, `GET /desk` (API) → `[]`, `/desk` page → 200 with the title no longer present; home page HTML contains `href="/desk"` confirming the nav link is live. `docs/self-hosting.md` gained a Desk section.

### Milestone 2 — user tags & tag provenance
- [x] User tags & tag provenance (2026-07-06, `docs/superpowers/plans/20260706-user-tags.md`) — new `items.user_tags text[]` column, independent of the AI-set `tags` column; enrichment never touches it, so your own tags survive re-enrichment. `PATCH /items/{id} {"userTags": [...]}` canonicalises (trim, lowercase, dedupe, cap 30 tags/50 chars) and persists via `SetUserTags` (user-scoped, 404 cross-tenant/missing). `search_tsv` rebuilt to index both `tags` and `user_tags` (weight B), so tag-only search works with no AI. Detail-page tag editor (add/remove chips) and card views showing the deduped union of AI + user tags shipped in web. Verified e2e 2026-07-06 against the local compose stack (`docker compose up -d --build api web`, noop provider): saved `danluu.com/why-benchmark/`, `PATCH /items/{id} {"userTags":["Mine","favourite","mine"]}` → `200` with `userTags` canonicalised to `["mine","favourite"]`; `GET` the item confirmed persistence and that AI `tags` stayed a separate (empty, noop) field; `GET /search?q=mine` returned the item (proves `user_tags` feeds `search_tsv`) — **found and documented, not fixed here:** `q=favourite` did *not* match due to a pre-existing `array_to_tsvector` stemming-mismatch bug (see Next), and the nested item in search results always shows `userTags:[]` because `search.sql` doesn't select the column (see Next). Import tag preservation covered below.
- [x] Import tag preservation (2026-07-06) — bookmark/CSV imports carry their tags into `user_tags` on the created item, immune to being overwritten by later enrichment (the `source` distinction is `tags` (AI) vs `user_tags` (everything else), simpler than the originally-planned per-tag `source` field). Verified e2e 2026-07-06: imported a Netscape bookmark file with `<A HREF="https://example.net/x" TAGS="research,go">` → `POST /import` `{imported:1}`; `GET /items` showed the created item with `userTags:["research","go"]` even though extraction failed for the fake domain (`status:"failed"`) — confirming tag preservation is independent of enrichment outcome.

### Milestone 2 — RSS/Atom feed subscriptions
- [x] RSS/Atom feed subscriptions (2026-07-06, `docs/superpowers/plans/20260706-rss-feeds.md`) — subscribe to a feed URL: current entries backfill as items immediately, then a River periodic job (`poll_feeds`, every 30 min, `RunOnStart`) re-polls for new entries — no new infra (runs in the existing worker binary) and no new dependency (stdlib `encoding/xml` parser, RSS 2.0 + Atom). New `internal/feeds` package (`Parse` + `Service{Add,Refresh,RefreshDue}`), migration `0005_feeds.sql` (`feeds` table, `unique(user_id,url)`), contract `POST/GET /feeds`, `DELETE /feeds/{id}` (Go + TS regenerated). Reuses `enrich.SafeHTTPClient` (SSRF-safe) and the importer's URL-dedup pattern so re-polling never double-saves. **Web:** `/feeds` page (add form, feed list with status pill + relative last-polled time, delete), "Feeds" topbar link, `/api/feeds` + `/api/feeds/[id]` cookie proxies. Verified e2e 2026-07-06 against the local compose stack (`docker compose up -d --build api web`, noop provider): `danluu.com/atom.xml` failed to fetch from inside the container (6.2MB feed, consistently truncated by the 20s fetch timeout — a container-network/bandwidth issue, not a feeds bug); fell back to `news.ycombinator.com/rss` per the plan's contingency — `POST /api/feeds` → `201` (title "Hacker News"); `GET /api/feeds` listed it with `lastStatus:"ok"`; within 15s `GET /items` (bearer) showed **30 new items** from the feed (27 enriched, 3 failed extraction — normal per-URL extraction misses, unrelated to feeds); confirmed the periodic job via `river_job` (row `kind=poll_feeds`, `state=completed`, timestamped at worker startup — `RunOnStart` fired); `DELETE /api/feeds/{id}` → `204`, `GET /api/feeds` → `[]`, and all 30 imported items **remained** in `/items`. `docs/self-hosting.md` gained a Feeds section. PDF capture, Omnivore-JSON import, and conditional-GET (etag) stay in Next.

### Milestone 2 — imports (file-based)
- [x] Bulk file import (2026-07-04) — `POST /import` (multipart `file`). New `internal/importer` package parses, dependency-free, three formats auto-detected by filename+content: **Netscape bookmark HTML** (browsers, Pocket, Raindrop, Pinboard, Instapaper — regex over `<A HREF>` + stdlib `html` entity decode), **CSV** with a URL column (Pocket/Raindrop — `encoding/csv`, header-name column detection), and a **plain newline-delimited URL list**. Handler creates a pending item per new/valid/not-already-saved URL and queues enrichment via the normal capture path (async, cheap models); de-dupes against existing URLs (`ListItemURLs` query) and within the file, so **re-import is idempotent**; caps at 10k items/request; returns `ImportResult {total, imported, skipped, failed}`. Contract regenerated (Go + TS). **Web:** `/import` page (file picker, live result summary with imported/skipped/failed tiles + supported-sources list), `/api/import` multipart proxy, "Import" link in the topbar beside Export JSON, `ImportResult` type. **Tests:** unit `TestParse*` (HTML/CSV/CSV-by-content/text/empty — green here); DB-backed `TestImportBookmarksIsIdempotent` (counts + item/job rows + idempotent re-import) and `TestImportRejectsEmptyFile` (run in CI). Verified 2026-07-04: `go build`/`vet` + importer unit tests green; web `tsc`/`build` green; drove the built `/import` page (headless Chromium) — uploaded a bookmark file, submitted, saw the 9/2/1 summary render. RSS + PDF + Omnivore-JSON split out to Next.

### Milestone 2 — reader mode
- [x] Distraction-free reader (2026-07-04) — new `/item/[id]/read` route: a single calm reading column on paper, no sidebar/rail, just a slim sticky top bar (← Back to card · Open original ↗). Mono kicker, large Newsreader title, italic-serif summary lead, and the archived body rendered as serif paragraphs at a generous 19px/1.85 over a ~680px measure; graceful empty/pending state with an open-original fallback. Detail page (`/item/[id]`) gained a "Read ↗" affordance (shown for text-forward types — article/product/book/recipe/note — with a body > 120 chars). Web-only, no backend/contract change. Verified 2026-07-04: web `tsc`/`build` green; drove the built pages (headless Chromium) — detail shows "Read ↗", reader renders the full article distraction-free. (Highlights-as-quote-cards from PRD §5.7 deferred — needs a `highlights` table + selection UI, out of scope for this slice.)

### Milestone 2 — Lenses
- [x] Lenses — schema + API + web (2026-07-04). A Lens = a named, saved search rule (`{q?, color?, types?}`); viewing one re-runs the rule so new saves appear automatically (no manual filing). **Schema:** migration `0004_lenses.sql` (`lenses(id, user_id, name, rule jsonb, created_at, updated_at)`, user-scoped index). **Contract:** `/lenses` (GET list, POST create), `/lenses/{id}` (GET, PATCH rename+re-rule, DELETE), `/lenses/{id}/items` (GET — runs the rule, returns a `SearchResponse`); new `Lens`/`LensRule`/`CreateLensRequest` schemas; Go + TS regenerated. **Store:** sqlc `lenses.sql` (Create/List/Get/Update/Delete), all `user_id`-scoped. **Handlers** (`internal/api/lenses.go`): rule validated + canonicalised on write (trim, dedupe types, reject unknown colour/type, require ≥1 signal) and stored as jsonb; `runLensRule` reuses `search.Run` for text/colour rules and falls back to recent-items-filtered-by-type for a types-only rule; a stored colour that later fails to parse degrades to an empty view, not a 500. **Web:** sidebar Lenses list is now live (real fetch, colour dot from the rule, active highlight; "New lens" link; dropped the mock rows + "soon"); `/lens/[id]` view (header with rule summary + live count, duplicate/delete, items grid); `/lens/new` create form (name, query, colour swatches, card-type chips) that seeds from the current search; "Save as lens" affordance added to the search-context strip; client mutations proxied via `/api/lenses`. **Tests:** unit `TestParseRule`/`TestMarshalRuleRoundTrip` (green here, no DB); DB-backed `TestLensCRUD`, `TestLensCreateValidation`, `TestLensItemsIsLiveView` (run in CI/laptop — no Docker daemon in web session). Verified 2026-07-04: `go build`/`vet` + rule unit tests green; web `tsc`/`build` green; drove the built web app (headless Chromium against a mock API) across home (live lenses sidebar), a parsed search showing "Save as lens", the empty + prefilled new-lens form, and a lens view rendering its matched items.

### Milestone 2 — colour + NL search UI
- [x] Web UI for colour search + NL parse (2026-07-04) — home search now sends `parse=true` whenever `q` is present, so the AI provider splits a fuzzy query into text + colour + card-type filters (degrades to plain FTS under noop). New adaptive `SearchContext` strip beneath the type filters: when a search was interpreted (or a `?color=` filter is active) it echoes the reading as "Understood as" chips — refined text (serif-italic pill, shown only when it differs from the raw query), a colour swatch + name pill (with an × to clear an explicit colour filter), and card-type pills; otherwise it reads "Search by colour". The right side always offers 8 brand/common colour swatches as one-tap colour search (`?color=<name>`, preserving `q`), active swatch ringed. `page.tsx` `getSearch` now returns `{items, understood}` and accepts a `color` param; `FilterStrip` carries `color` through type-chip links. New `lib/colors.ts` mirrors the backend `namedColors` map (`apps/api/internal/search/color.go`) to resolve a term (hex or name) to a swatch hex. No contract change — the generated client already exposes `understood`. Verified 2026-07-04: `tsc`/`lint`/`build` green; drove the built app (headless Chromium) against a mock API across three states — recents ("Search by colour" + swatches), NL query `cozy green cabins` → "Understood as" `"cabins"` + green + Image/Article chips, and `?color=cobalt` → cobalt chip with clear + cobalt swatch ringed.

### Milestone 2 — NL query parsing
- [x] NL query parsing (2026-07-04) — `ParseQuery` is now structured: provider interface returns `ParsedQuery{Text, Color, Types}` instead of a bare string. Gemini + OpenAI-compatible providers gained real JSON-mode implementations (shared `parseQueryInstruction` prompt; model-proposed card types sanitised against the openapi enum); noop/fake stay identity (`{Text: q}`) so FTS-only search keeps working with no AI. `/search` gained an opt-in `parse=true` param: it runs `ParseQuery`, then fuses the split text + colour into the existing RRF search and narrows by card-type filter (new `types` arg on `search.Run`, applied post-ranking pre-limit). Explicit `color` param still wins over a parsed colour; a parse failure or empty split falls back to the raw query so search never breaks. Response shape changed from a bare `SearchResult[]` to `SearchResponse {results, understood?}` — `understood {text,color,types}` echoes what was actually searched, ready for the "understood-as chips" web overlay (still to come). Contract regenerated (Go + TS). Verified 2026-07-04: full `go test -p 1 ./...` green against local Postgres+pgvector (new DB-backed `TestSearchItemsParseSplitsQuery` drives `parse=true` end-to-end — colour+type split, understood echo, type filter drops the article; `TestRunFiltersByType`; unit `TestOpenAI_ParseQuery` covers JSON parse + unknown-type drop); web `tsc`+`build` green; real binary smoke test confirmed `parse=true` with noop degrades to `understood:{text:q}` and still returns FTS hits.

### Milestone 2 — colour search
- [x] Colour search backend (2026-07-04) — `/search` gains an optional `color` param (hex `#RRGGBB`/shorthand or named colour incl. Openmind accents cobalt/terracotta/gold/green); `q` now optional, ≥1 of q/color required. New `search/color.go`: hex+name parse → sRGB→CIELAB, ΔE*76 nearest-palette-colour ranking over the stored `palette text[]`, fused into the existing RRF alongside FTS+vector so text+colour queries combine. New tenant-scoped `ListItemsWithPalette` query. Contract regenerated (Go + TS client). Unit tests (parse/ΔE ordering/ranking) green; DB-backed tests (`TestColorSearch*`) written — run in CI/laptop (no Docker daemon in web session). Web UI + NL colour parsing still to come.

### Milestone 1 — web design pass
- [x] Warm-editorial reskin of the web app (`docs/superpowers/plans/20260704-design-pass.md`) — tokens/fonts/shell (Task 1), type-aware cards + image fallback (Task 2), topbar/filter strip/capture/search (Task 3), palette dots server-extraction + render (Task 4), **card detail reader restyle (Task 5)**, visual verification (Task 6). Detail page (`/item/[id]`) rebuilt to the reader look: bright reader panel on canvas, mono meta line, large Newsreader title, serif summary lead, cobalt "Open original ↗", right-hand rail (palette swatches + tags + "archived locally"), type-aware bodies (article/product/book/recipe/video/tweet hero + summary + body, quote = gold-glyph italic serif, note = serif body, image = large image). Verified 2026-07-04: `pnpm --filter web build`+`lint` green; drove the built page with headless Chromium across article / broken-image / quote / note / image fixtures — all 200, **zero `<img>` tags** (images painted as background-over-gradient, so a 404 hero reveals the type gradient, never a broken-image glyph), reader shell + palette rail present. Remaining M2 design screens (out of scope): Ledger view, Drift, Desk, functional Lenses, search overlay with understood-as chips + colour swatches, ⌘K quick-capture palette.

### Milestone 1 — image upload
- [x] Strip EXIF/GPS/XMP/IPTC metadata from uploaded images (privacy) — e2e verified 2026-07-04: crafted a JPEG with an injected APP1 `Exif\0\0`+fake GPS/TIFF segment, `POST /api/assets` → 201, `GET /api/assets/<id>` → downloaded bytes contain zero `Exif\0\0` occurrences, file still starts `FFD8`/ends `FFD9`, and `jpeg.Decode` succeeds (bounds unchanged, 2x2). AVIF removed from the upload allowlist (fake `ftyp`-branded `avif` file → `POST /api/assets` → `415 unsupported image type`) pending lossless AVIF metadata stripping. Docs updated (`docs/self-hosting.md`, image-upload spec).
- [x] Assets table + image upload capture — e2e verified 2026-07-04: `POST /api/assets` (multipart, cookie auth) → 201 image card with `leadImageUrl=/assets/<id>`; `GET /api/assets/<id>` → 200 `image/png` bytes with `X-Content-Type-Options: nosniff`; unauth `GET` → 401; item enriched within ~8s (`GET /items` status `enriched`). Fixed a blocking bug found during e2e: distroless `nonroot` API image couldn't write to a freshly-initialised `assetsdata` named volume (root:root ownership) — `apps/api/Dockerfile` now pre-creates `/data/assets` chowned to uid/gid 65532 so Docker seeds the volume with writable ownership. Oversize (`413`) covered by existing unit tests, not re-verified manually.

### Milestone 1 — AI adapter
- [x] AI adapter: OpenAI-compatible client + ordered fallback chain (gemini → openai-compatible → noop), per-provider rate-limit config, 429 = fall over not fail — e2e verified 2026-07-03: openai (unreachable `.invalid` base URL) → noop fallover, item enriched with empty summary, chain log confirmed (`ai chain: provider error, failing over provider=openai op=summarise`).

### Research
- [x] Karakeep repo deep-dive (docs/research.md, 2026-07-03): multi-container TS stack (app + Meilisearch + Chrome + workers) vs our single binary; no vector search in core; AGPL — interop fine, never copy code. Learn from: capture breadth, importers, feeds. Avoid: headless-Chrome archival weight, unvirtualised unbounded views.
- [x] openapi.yaml contract + codegen and items/embeddings schema — delivered across Milestone 0 and later slices (superseded the original Now items)

### Milestone 0 / M1 — masonry grid perf spike
- [x] Masonry grid perf spike (docs/research.md, 2026-07-03): CSS-columns masonry holds 60fps easily — median 120fps (120Hz-capped), 0 dropped frames, 8.3ms avg frame at both 500 and 1000 cards. Verdict: **keep CSS columns for M1, do not virtualise**; add `content-visibility: auto` as the cheap first lever only if a future unbounded view shows first-paint/memory pressure.

### Milestone 1 — card detail + export
- [x] Card detail + reader view (type-aware renderers) — e2e verified 2026-07-03: `GET /item/<id>` with logged-in cookie returns 200 HTML containing the item's title.
- [x] JSON export — e2e verified 2026-07-03: `GET /api/export` with Bearer token returns a JSON array with non-empty `body` per item; item removed from the array after `DELETE /api/items/<id>` (204).

### Milestone 1 — extension
- [x] WXT extension: save page / selection / image, options page (instance URL + token, validate/save), e2e verified 2026-07-03: browser extension capture (url + note) via Bearer token auth against a fresh local build (`pnpm --filter extension build`); server-side confirmed with `GET /api/auth/check` (200 correct token / 401 wrong token) and `POST /api/items` for both `{url}` and `{note}` payloads (201).
- [x] Extension polish: popup quick-tag after save + recent-saves list, `Ctrl/Cmd+Shift+S` save-page shortcut. e2e verified 2026-07-06 against local `docker compose` (Bearer path, no cookie) on `http://localhost:3000/api/*`: `POST /api/items {url}` → 201 (+id); `PATCH /api/items/{id} {userTags:["from-extension"]}` → 200 and the item carries the tag; `GET /api/items?limit=5` → 200 array incl. the tagged item; `GET /api/auth/check` → 200 (right token) / 401 (wrong token). Confirmed `apiFetch` honours the incoming `Authorization: Bearer` header. **Fixed** a proxy gap: the web `/api/items` route had only `POST` (no `GET`), so recent-saves returned 405 → empty list; added a `GET` pass-through handler (`apps/web/app/api/items/route.ts`) and re-verified. README documents popup features, shortcut + rebind, load-unpacked + settings. User must reload the unpacked extension (`.output/chrome-mv3`) and redeploy the web app to pick up the GET-proxy fix.

### Milestone 1 — mobile
- [x] Expo (SDK 57) mobile app: share-sheet-first capture. Standalone `apps/mobile` (own lockfile, not in the pnpm workspace) — expo-router tabs (Library / Capture / Settings), token login (`expo-secure-store`, web falls back to `localStorage`), `lib/api` (saveItem/listItems/checkToken) against the instance's `/api/*` with a Bearer token. **Share sheet** via `expo-share-intent` (config plugin in `app.json`: iOS `public.url`/`public.plain-text` activation + Android `text/*` SEND) → on a received share, `ShareIntentGate` (root `_layout`) routes to Capture pre-filled; native module no-ops on web so the export still builds. Verified 2026-07-06: `tsc --noEmit` clean; `expo export --platform web` builds (8 static routes); web preview driven via headless Chromium at 440×900 — Settings / Capture / Library render clean with the warm palette + tab bar (screenshots `.superpowers/sdd/shot-mobile-{settings,capture,library}.png`). **Verification scope:** UI verified via web preview + types + web export only. The **share extension is device-only** (can't run in Expo Go or web) — it's wired + configured + documented (`apps/mobile/README.md`), and the user runs a dev build (`npx expo run:ios|android` / EAS) to exercise it on-device. Completes the user's "both" (extension + mobile) capture surfaces.

Next slice: mobile Milestone 2+ ideas (offline capture queue, push, image/camera-roll capture, biometric lock, app-store submission) — deferred; capture + library is the v1 phone surface.

### Milestone 1 — expose-ready web
- [x] SSRF hardening for extractor fetches (private-IP dialer guard, redirect re-check) + bearer auth + per-IP rate limiting before public exposure
- [x] Web container: `output: standalone` + monorepo Dockerfile; compose `web` service on `127.0.0.1:3000` (`API_URL=http://api:8080`, shared `OPENMIND_TOKEN`, bearer-cookie login validated against the API). docs/self-hosting.md expanded (web service, token setup, map domain to web:3000 only). Verified e2e 2026-07-03: login 200 / wrong-token 401, add URL + note, home + `?q=` search render enriched cards.

### Milestone 0 — Week-0 spike (committed, not killed)
- [x] Gemini provider verified live on deploy box 2026-07-03: real article → enriched with summary + 5 tags; /search?q=entrepreneurship (AI-tag-only term) returns it
- [x] Scaffold monorepo: Taskfile, go.work, pnpm workspaces, turbo.json, docker-compose (postgres + pgvector)
- [x] Pipeline end-to-end in one binary: URL → extract → summary/tags → pgvector embed → hybrid search returns it (verified e2e with noop provider 2026-07-03)
- [x] Extraction bake-off on 18 real URLs (docs/bakeoff-results.md): **winner = go-trafilatura** (best body extraction, acceptable latency); kept as default. Jina Reader kept as a config-gated fallback for later. go-readability close second.
- [x] Decide: Next.js vs TanStack Start → **Next.js locked in** (App Router, React 19)
- [x] E2e verification + wrap-up: `docker compose build` + noop parity run green; `task test` + `task lint` green (real-Gemini leg deferred to first deploy)

### Pre-Milestone 0
- [x] PRD v1 (docs/PRD.md)
- [x] Grid mockup (docs/design/openmind-mockup.html)
- [x] CLAUDE.md
